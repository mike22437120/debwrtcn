config DEBWRT_DEBIAN_RELEASE
        string
        default "sid" if DEBWRT_DEBIAN_RELEASE_SID
        default "squeeze" if DEBWRT_DEBIAN_RELEASE_SQUEEZE
        default "lenny" if DEBWRT_DEBIAN_RELEASE_LENNY
        default "sid"

